#include "main.h"

/**
 * _puts - prints a string
 * @str: this is the string character
 * Return: nothing
 */

void _puts(char *str)
{
	char *str;

	str = "I don't fear computers";
	_puts("str\n", str);
}
